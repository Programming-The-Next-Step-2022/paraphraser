library("project.margarida")

context("Core Translate Functionality")

# change for correct inputs for each argument
test_that("translate() accepts string input only", {
  expect_error(translate(34,3))
  expect_error(translate(TRUE,FALSE))
})

test_that("translate() returns a different string from input text", {
  expect_false(isTRUE(all.equal(input_text, output_text)))
})

# add for third argument as well
test_that("translate() accepts only available language codes as second argument", {
  expect_error(translate("text", "TH"))
})

test_that("translate() returns text in target language", {
  # variables don't exist yet for this test
  # expect_equal(target_lang, output_lang)
})

test_that("translate() returns correctly translated text", {
  expect_equal
})

test_that("translate() formality level is properly set", {
  
})

test_that("", {
  
})
